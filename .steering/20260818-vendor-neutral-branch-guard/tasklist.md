# タスクリスト

<!-- main-edit-ok -->

> **司令塔が直接実装する作業です。** 理由: (1) テンプレート自身のハーネス改修であり `check-implementation-phase.sh` の脱出弁が想定するケースそのもの、(2) ベンダー中立ガードレールの**参照実装(1 例目)**にあたり、`model-strategy.md` の「参照実装は司令塔が書く」に該当、(3) design.md にスクリプト全文を書き切っており、fork に渡しても設計判断はゼロで spawn の固定費(15〜20k トークン)だけが増える。

## フェーズ1: 共有スクリプトの新設

- [x] `.claude/scripts/check-protected-branch.sh` を design.md の実装どおり作成する
- [x] `chmod +x` で実行権限を付ける
- [x] 使い捨て git リポジトリで検証マトリクスを通す
  - [x] #1 保護ブランチ上 → exit 1 + stderr に案内
  - [x] #2 作業ブランチ上 → exit 0 / 無出力
  - [x] #3 `branch-policy.json` 無し → exit 0
  - [x] #4 detached HEAD → exit 0
  - [x] #5 git リポジトリ外 → exit 0
  - [x] #6 `protectedBranches` が空配列 → exit 0

## フェーズ2: `.husky/pre-commit` への組み込み

- [x] `.husky/pre-commit` を design.md の内容に差し替える
- [x] 保護ブランチ上で直接実行し、`lint-staged` に到達せず中断することを確認する
- [x] 作業ブランチ上で実行し、検査を通過して `lint-staged` に進むことを確認する
- [x] 共有スクリプトが存在しない場合でも `lint-staged` に進むことを確認する

## フェーズ3: `check-branch-policy.sh` の重複排除

- [x] インラインの `protectedBranches` 判定を共有スクリプトの呼び出しに置き換える
- [x] 保護ブランチ上のコミットが exit 2 でブロックされることを確認する(挙動の後退なし)
- [x] 作業ブランチ上のコミットが素通しされることを確認する
- [x] PR 作成コマンドの base 検査が従来どおり動くことを確認する(回帰)

## フェーズ4: ドキュメント反映

- [x] `docs/template-dev/codex-delegation-plan.md` §11 の段階1 を完了として更新する
- [x] 同 §8 に「husky は `npm install` 後にしか有効にならない」「`--no-verify` で素通しできる」の制約を追記する
- [x] `.claude/rules/lead/branch-and-tickets.md` の「強制層は 3 段」の記述を実態(4 段)に合わせる

## フェーズ6: レビュー指摘の修正(追加)

- [x] 呼び出し側のフェイルオープン欠陥を修正する(`case $? in 1)` で違反のみブロック)
  - [x] `.claude/scripts/check-branch-policy.sh`
  - [x] `.husky/pre-commit`
- [x] フェイルオープンの検証ケースを追加する(不在・内部エラー × hook/husky = 5 件)

## フェーズ7: ハーネス自壊検知の追加(追加)

- [x] SessionStart hook に `.husky/pre-commit` が共有スクリプトを呼んでいるかの検査を追加する
  - 根拠: `.husky/` は manifest の `merge` 対象=手動統合のため、同期ミスで呼び出しごと落ちても誰も気づかない
- [x] `core.hooksPath` 未設定(husky 無効)の検知を追加する
- [x] 正常時に無言・呼び出し削除時に警告・復元時に無言、を確認する

## フェーズ8: 監査で見つかった破綻要因の修正(追加)

3 巡目の監査(計画と実装の突き合わせ)で発見した 8 件。

- [x] 自壊検知を CI にも置く(`ci.yml` の Harness integrity に `.husky/pre-commit` の検査を追加)
  - 根拠: SessionStart は Claude 専用。**モード C は Claude が起動しない期間そのもの**であり、検知自体がベンダー依存では意味がない
  - [x] 正常時 exit 0 / 呼び出し削除時 exit 1 を使い捨てディレクトリで確認
- [x] `design.md` の完成マーカー規約を**読み込まれる場所**に載せる
  - [x] `.claude/skills/steering/templates/design.md` に `<!-- status: draft -->` を既定で埋める
  - [x] `.claude/skills/steering/SKILL.md` モード1 に `ready` への変更手順と判断基準を追記
  - 根拠: 規約が `docs/template-dev/`(CLAUDE.md が明示的に読み込み対象外と宣言)にしか無かった
- [x] プラン §12.2 の手順順序を修正(CI は PR 作成後にしか走らないため「CI が緑なら PR」は因果が逆)
- [x] プラン §9・§12.3 に「push する主体は人間」を明記(Codex はネットワーク無効かつ push 禁止)
- [x] プラン §2.5 の終了コードを exit 2 → exit 5 に修正(§3.2 との不一致)
- [x] プラン §3.2 に委託前の入口検査(検証コマンドの実行可否・`core.hooksPath`)を追加
  - スタック非依存にする制約つき(`delegate-codex.sh` は `owned` = 全プロジェクトに配られる)
- [x] プラン §8.2 の `.harness/` gitignore 範囲を `mode` / `codex-runs/` に限定(`decisions.jsonl` は永続ログ)
- [x] プラン §12.3 の手順番号の重複を修正
- [x] プラン §2.6 に draft PR の機構的裏づけを追記(`draft == false` 条件がモード B の節約の実体)

## フェーズ9: 4 巡目の監査で見つかった破綻要因の修正(追加)

フェーズ8 の修正**そのもの**を点検して見つかった 3 件。

- [x] `implement-ticket` に完成マーカーの入口検査を追加する
  - 根拠: フェーズ8 で steering スキルに「委譲は入口検査に使う」と書いたが、`implement-ticket` 側に検査が無く**存在しない挙動を規約として書いてしまっていた**
  - 差し戻しは「失敗」ではなく「判断待ち」(回復手段が違う)
- [x] 「マーカーが無ければ `draft` とみなす」を「**印があるものだけ検査する**」に改める
  - 根拠: 既存 `.steering/` 3 件すべてにマーカーが無く、当初の規則では**このリポジトリ自身の委譲経路が即座に止まる**
  - Codex と Sonnet fork で同じ規則にする(交換可能性 §4.4)
- [x] CI の自壊検知を `quality` から独立ジョブ `harness-integrity` に分離する
  - 根拠: `quality` の最終ステップにあると lint / テスト失敗時に fail-fast で実行されない。**ガードレールが消えている PR ほど他も落ちやすく、最も検知したい場面で検知できない**
  - [x] 正常時 exit 0 / 呼び出し削除時 exit 1 / husky 不在時 exit 0 の 3 ケースを確認

## フェーズ10: 5 巡目の監査で見つかった破綻要因の修正(追加)

計画・実装・ドキュメントを突き合わせて見つかった 8 件。

- [x] 完成マーカーの規約がテンプレートだけ逆だったのを修正する
  - 根拠: フェーズ9 で「印が無ければ通す」に反転させたのに `templates/design.md` の説明文だけ「`draft` とみなす」のまま残っていた。**司令塔が design.md を書くとき目に入るのはテンプレート側**であり、AGENTS.md をここから派生させると Codex だけ厳しい規則で動く
- [x] 自壊検知の穴を塞ぐ(スクリプトごと消えると検知自体がスキップされる)
  - 根拠: `[ -f script ] && ! grep ...` の形は**層が完全消滅した最悪のケースで exit 0** になる。「層が無いことより気づけないことの方が悪い」がそのまま裏返っていた
  - [x] `.claude/hooks/session-start.sh`(存在と呼び出しの両方を見る)
  - [x] `.github/workflows/ci.yml` の `harness-integrity`(同上)
  - [x] `.husky/pre-commit`(スクリプト不在時に stderr へ警告。フェイルオープンは維持)
- [x] モード C が §7.1 のモード判定規則と矛盾していたのを解消する
  - 根拠: 「モードは `delegate-codex.sh` が渡す。無ければモード A(コミットしない)」なのに、モード C は `delegate-codex.sh` を通らない。**規約どおりなら縮退モードでコミットが拒否され、モード C が成立しない**
  - 解決: `.harness/mode` を Codex も読む単一ソースにし、優先順位を「プロンプト > `.harness/mode` > モード A」に固定する
- [x] README の「3 段構え」を 4 段に更新する
- [x] `development-guidelines` スキルの強制層リストに husky 層を追加する
  - 根拠: **各プロジェクトの `docs/development-guidelines.md` の生成元**であり、放置すると全プロジェクトに古い説明が配られる
- [x] `check-implementation-phase.sh` の通過パスに `.husky/*` を追加する
  - 根拠: コメントは「ハーネスの編集は通す」なのに `.husky/` が入っておらず、司令塔が触るたびに `<!-- main-edit-ok -->` を要求される。あの脱出弁は消し忘れると実装フェーズ強制が丸ごと死ぬ最も危険な弁
- [x] 入口検査の強制力が Codex 側だけ機械的である非対称を、設計判断として明記する
- [x] モード C の入口検査を `.codex/prompts` 側にも写す(§7.3)
  - 根拠: モード C は `delegate-codex.sh` を通らない唯一の経路で、§3.2 の機械的検査がそこだけ効かない
- [x] §13 の未確認項目に優先度を付ける(#2・#3 は骨格に効く)
- [x] CHANGELOG に 2026-08-19 のエントリを追加する
  - 根拠: `.husky/pre-commit` は `merge` 対象なので `[manual]` が無いと取り込む側に伝わらない。段階1 全体のエントリがそもそも欠けていた

## フェーズ11: 6 巡目の監査で見つかった破綻・欠落(追加)

フェーズ10 の修正そのものと、変更が波及する先を点検して見つかった 5 件。

- [x] `.devcontainer/devcontainer-lock.json` を `.gitignore` に追加する
- [x] **新規スクリプトが git 上で実行権限なしになる問題を修正する(破綻)**
  - 根拠: この devcontainer は `core.fileMode=false`。ディスクが `-rwxr-xr-x` でも **index には 100644 で入る**ことを実測。CI の `harness-integrity` は実行権限を必須にしているため、**この変更を含む最初の PR が必ず落ちる**。しかも落ちる原因は「守るために足したファイル」自身
  - `core.fileMode=true` は不採用(既存 98 ファイルに偽のモード差分が出る)。`git update-index --chmod=+x` で対象ファイルだけ立てた
  - [x] SessionStart hook に index 側の権限検知を追加(ディスクだけ見る既存検査では素通しする)
  - [x] CI のエラーメッセージに復旧コマンドを追記(`chmod +x` では直らないことを明示)
  - [x] 権限を落とした状態で検知され、戻すと無言になることを使い捨てリポジトリで確認
- [x] `implementer` エージェント定義にも完成マーカーの入口検査を追加する
  - 根拠: 検査はスキル側にしか無く、**エージェントの「手順」1〜7 と「停止条件」には存在しなかった**。フォークはどちらも読むため、片方に無い規約は弱い。§4.4 の交換可能性は「同じ規則で動くこと」を要件にしている
- [x] `/add-feature` の `判断待ち` 分岐に、マーカー起因の差し戻しの回復手順を追記する
  - 根拠: 既存の指示は「`design.md` に追記して再実行」。マーカーが `draft` のままだと**追記しても同じところで止まり続ける**無限ループになる
- [x] `/kickoff` のテンプレート由来 `.steering/` 削除を機械的検出に変える
  - 根拠: 削除対象を名前パターン(`*-fork-implementation-phase` 等)で例示していたが、**今回 3 件目のディレクトリが増えた**。名前で数え上げる方式は増えるたびに取りこぼす。`main-edit-ok` を含む `tasklist.md` を持つものは例外なくテンプレート由来なので、それを正とする

## フェーズ5: 品質チェック

- [x] `/check`(test-runner に委譲)の全チェックがパスすることを確認
  - この変更はシェルスクリプトのみで TypeScript に影響しないが、回帰がないことを機械的に確認する
- [x] 実装後の振り返り(このファイルの下部に記録)

---

## 実装後の振り返り

### 実装完了日

2026-08-18

### 計画と実績の差分

**計画と異なった点**:

- **`npm ci` の実行が必要だった。** 作業開始時点で `node_modules` が無く、`core.hooksPath` も未設定で **husky 自体が無効**だった。つまり移植前の `.husky/pre-commit`(`lint-staged` のみ)も動いていなかった。これは design.md「既知の制約」1 の実例そのもので、設計の想定内。`npm ci` 後は `core.hooksPath=.husky/_` となり実地で有効化された
- **テストを本リポジトリではなく使い捨て git リポジトリで実施した。** 保護ブランチ(`main`)上の挙動を検証するには `main` に切り替える必要があり、作業ツリーを汚す。スクリプトと `pre-commit` をコピーした一時リポジトリで全ケースを回した

**新たに必要になったタスク**:

- なし(フェーズ1〜5 を計画どおり完走)

**技術的理由でスキップしたタスク**:

- なし

### 学んだこと

**技術的な学び**:

- **判定の実体を 1 ファイルに集約する構造が有効だった。** `latest-steering.sh` を hook・fork・スキルで共有しているのと同型。今回は `check-protected-branch.sh` を `.husky/pre-commit`(ベンダー非依存)と `check-branch-policy.sh`(Claude 専用)の両方から呼ぶ形にした。終了コードの読み替え(1 → 2)を呼び出し側に持たせることで、共有側は用途を知らずに済む
- **フェイルオープン設計は「セキュリティ境界として扱わない」という前提とセット。** ポリシーファイルや `jq` が無い環境でコミット不能にしない代わりに、権限境界は GitHub のブランチ保護に置く。この割り切りを design.md とプラン §8 の両方に明記した
- **ガードレールのメッセージにバイパス手段(`--no-verify`)を書かない。** ブロックしている当の画面で回避策を案内するのは本末転倒。存在自体はドキュメントに残す

**プロセス上の改善点**:

- design.md にスクリプト全文を書き切ったため、実装が機械的な作業になった。fork に渡しても設計判断ゼロで通せる粒度だったが、今回は参照実装として司令塔が直接実装した(`<!-- main-edit-ok -->` の脱出弁を使用)

### レビューで発見・修正した自作の欠陥

**共有スクリプトの呼び出し側がフェイルオープンになっていなかった。**

- 当初の実装は `if ! bash .claude/scripts/check-protected-branch.sh; then exit 2; fi` としていたが、これは**違反(exit 1)以外もすべてブロック**する。スクリプト不在(exit 127)や内部エラーのときに、保護ブランチでなくても全コミットが止まる
- 共有スクリプトは 4 条件でフェイルオープンする設計なのに、**呼び出し側が fail-closed** という設計と実装の不整合だった。テンプレート同期の途中状態・権限落ち・パス解決失敗で開発が止まる事故につながる
- 修正: 両方の呼び出し元で `case $? in 1) ... esac` とし、**exit 1 だけを違反として扱う**
- 検証を 15 → 20 ケースに拡張(不在 × hook/husky、不在かつ保護ブランチ、内部エラー × hook/husky)。全 PASS
- **学び**: 「フェイルオープン設計」は共有スクリプト側だけでは完結しない。**終了コード契約は呼び出し側にも同じだけの責任がある。** 判定を 1 箇所に集約したことで、逆に「呼び出し側が契約を誤読する」新しい失敗モードが生まれていた

### 監査で発見した「自分で作った層」の弱点

**自壊検知そのものがベンダー依存だった。**

- フェーズ7 で追加した「`.husky/pre-commit` が共有スクリプトを呼んでいるか」の検査は SessionStart hook = **Claude 専用**にしか置いていなかった
- ところがこの層が守りたいのは「Claude が居ない期間(モード C)」である。**検知が必要な期間に検知役が動かない**という構造で、「層が無いことより気づけないことの方が悪い」という自分の論理がそのまま跳ね返っていた
- 修正: 同じ検査を CI の Harness integrity にも置いた。SessionStart = 早期警告 / CI = クライアント非依存の最終検証、という二層に整理
- **学び**: ベンダー中立な層を作るときは、**その層の監視役もベンダー中立か**を必ず確認する。守る対象だけを中立化しても、監視が依存していれば実効性は監視側の可用性に落ちる

### 発見した既存の不具合(スコープ外・要別対応)

**`check-branch-policy.sh` の `CMD_START` 正規表現が markdown のコードスパンを誤検知する。**

- `CMD_START` はコマンド置換を拾うためにバッククォートを起点文字に含んでいる。そのため文書中のコードスパン(バッククォートで囲んだ PR 作成コマンドなど)が「コマンド位置のコマンド呼び出し」と誤判定される
- **今回この振り返りと `requirements.md` を heredoc で書こうとして実際に 2 回ブロックされた**(Write / Edit ツールに切り替えて回避)
- 影響: PR 作成やコミットのコマンドに言及するドキュメントを Bash 経由で書けない。このテンプレートは自分自身のワークフローを文書化するため、遭遇頻度が高い
- 同ファイル冒頭が「文字列パターンによるベストエフォート」と断っているとおり既知の割り切りではあるが、**バッククォートだけは誤検知の実害が大きい**。起点文字から外すか、対になるバッククォート(コードスパン)を除外する前処理を検討する

### 5 巡目の監査(フェーズ10)で見つかったこと

**同じ失敗が 3 度形を変えて出た: 「検査を守るための条件が、検査そのものを無効化していた」。**

- フェーズ7 で自壊検知を作り、フェーズ8 で「検知役がベンダー依存だった」と直し、フェーズ10 で「**検知の前提条件(スクリプトの存在)が満たされないと検知自体がスキップされる**」と直した。`[ -f script ] && ! grep ...` という形は、**層が完全に消滅した最悪のケースで exit 0** になる
- 一般化すると **「X が壊れていないか」を X の存在を前提に検査してはいけない。** 検査対象の消失は、検査が捕らえるべき事象のうち最も重いものである
- 検証を A〜E の 5 ケースに拡張し、C(スクリプト削除)と D(スクリプト削除・呼び出し残存)が exit 1 になることを確認した

**規約を反転させたとき、写像先の全部を数えていなかった。**

- フェーズ9 で完成マーカーの規則を「無ければ draft」→「印があるものだけ検査」に反転させたが、写像先は 4 箇所(プラン §2.5 / steering SKILL / implement-ticket SKILL / **テンプレート本文**)あり、テンプレート本文だけ旧規則のまま残った
- 最も参照されるのがテンプレート本文であり、しかも AGENTS.md をここから派生させる計画(§7.1)なので、放置すると **Codex だけが旧規則で動く**ところだった
- **学び**: 規約を反転させたら「この規約が書かれている場所」を grep で数え上げてから直す。人間向けの説明文は実装より写像先が多い

**「Claude 不在時の代替」を設計するとき、Claude 経由の伝達手段に依存していないかを毎回問う。**

- モード C は `delegate-codex.sh` を通らないのに、モードの伝達を `delegate-codex.sh` のプロンプト注入だけに頼っていた。規約どおり動くと **Codex はモード A に落ちてコミットを拒否し、縮退モードが成立しない**
- これはフェーズ8 の「検知役がベンダー依存だった」と**同じ構造の誤り**(依存の向きを見落とす)。`.harness/mode` をファイルとして両者が読む形に直した
- **学び**: ベンダー中立化のレビュー観点は「守る対象」だけでなく、**監視・伝達・入口検査のすべて**に当てる。モード C で通らない経路は `delegate-codex.sh` だけではなく、そこに載せた検査もすべて通らない

### 6 巡目の監査(フェーズ11)で見つかったこと

**ガードレールを足すと、そのガードレール自身が最初の違反者になりうる。**

- CI の `harness-integrity` は `.claude/scripts/*.sh` に実行権限を要求する。ところがこの環境は `core.fileMode=false` で、**新規スクリプトは index に 100644 で入る**。つまり「保護ブランチを守るために新設したスクリプト」が、「ハーネスの健全性を守るために新設したジョブ」を落とす構図だった
- ディスクの権限しか見ない既存検査では検知できず、**クリーンなクローンで動く CI だけが気づく = 気づくのが最も遅い層でしか検知できない**。フェーズ8・10 と同じ「検知の位置」の問題がまた出た
- **学び**: 検査を足したら、**その検査を最初に通るのは自分の変更**であることを必ず確認する。ローカルで緑でも、CI はクリーンなクローンなので前提が違う

**規約の写像先は「ロードされる場所」だけでなく「同時にロードされる全部」を数える。**

- 完成マーカーの検査は `implement-ticket/SKILL.md` に書いたが、フォークは `implementer.md`(エージェント定義)も同時に読む。そちらの「手順」と「停止条件」には無く、**手順書として矛盾していた**
- フェーズ8 で「読み込まれる場所に置く」を学んだつもりだったが、**読み込まれる場所が複数ある**ことまでは数えていなかった

**名前のパターンで数え上げる規則は、対象が増えるたびに壊れる。**

- `/kickoff` の「テンプレート由来 `.steering/` を削除」は名前パターンの例示に頼っていた。今回でテンプレート側の作業記録が 3 件目になり、取りこぼしの確率が上がっていた
- `main-edit-ok` という**事実**で判定する形に変えた。名前は増えるが、脱出弁の有無は増えない

### 次回への改善提案

- **段階2(`delegate-codex.sh`)でも「シェルから叩ける単一実体」パターンを踏襲する。** 呼び出し元(Claude / 人間 / Codex)が変わっても判定と挙動が 1 箇所に集約されている状態を保つ
- 上記の `CMD_START` 誤検知を独立したチケットとして起票する
- Codex 契約後、`.husky/pre-commit` が Codex のコミットを実際にブロックすることを実機で 1 回確認する(今回はシミュレーションのみ)

## フェーズ12: 7 巡目の監査で見つかった破綻要因の修正(追加)

- [x] `.husky/pre-commit` のフェイルオープンが `sh -e` で無効化されていた問題を修正(終了コードを `&& / ||` のリスト内で受ける)
- [x] `npx lint-staged` を `lint-staged` の直呼びに変更(ネットワーク制限環境での不可解な失敗を回避)
- [x] 保護ブランチ検査の 4 パターン検証(main で中断 / feature で通過 / guard 構文エラーで素通し + 警告 / guard 消失で素通し + 警告)
- [x] `protectedBranches` の空洞化検知を SessionStart hook と CI の `harness-integrity` に追加
- [x] `git ls-files -s` の awk 分割をタブ区切りに変更(パスに空白が入っても切れないように)
- [x] `codex-delegation-plan.md` §8 に「ブランチ保護という受け皿がこのリポジトリには存在しない」を追記し、3 択を明示
- [x] `codex-harness.html` を再構成: 3 モードの実行ランブック(縦割り 13 フェーズ × 3 モード)を §02 として新設し、既存セクションを繰り下げ
- [x] `codex-harness.html` に注釈(メモ)機能を追加。本文選択 → メモ、一覧・書き出し・読み込み・印刷時の巻末注
- [x] CHANGELOG に同日追補として 3 項目を追加(`.husky/pre-commit` は `merge` 対象なのでセット取り込みを明記)

## 7 巡目の監査(フェーズ12)で見つかったこと

**「判定の実体を 1 箇所に集約する」は、呼び出し側のシェルオプションまでは共有しない。**

- フェーズ8 で「呼び出し側がフェイルオープンになっていない」を修正し、`case $? in 1) exit 1 ;; esac` を 2 箇所(`check-branch-policy.sh` と `.husky/pre-commit`)に写した。ところが husky はフックを **`sh -e`** で実行するため、`.husky/pre-commit` 側では `bash "$GUARD"` が非ゼロを返した時点でシェルが終了し、**`case` は死にコードだった**
- PreToolUse hook 側(`set -e` 無し)では同じコードが正しく動いていたぶん、片方だけが壊れていることに気づけなかった。**同じ 3 行でも、実行環境によって意味が反転する**
- 結果として「内部エラーはフェイルオープン」という設計の真逆(`bash` 不在・構文エラー・権限落ちで**全コミットが不能**)になっており、しかも回復手段が `--no-verify` しかない = **この作業が防ごうとしている「静かな自壊」の最も派手な裏返し**だった
- **学び**: 終了コード契約を書くときは、**呼び出し側がどのシェルで・どのオプションで起動されるか**まで契約に含める。「共有スクリプトがフェイルオープンなら全体がフェイルオープン」は成り立たない

**同じ配列を読む層をいくら増やしても、その配列が空になれば全層が同時に消える。**

- 自壊検知は「呼び出しがあるか」「実体があるか」まで見るようになっていたが、**`protectedBranches` が空**というケースは 3 層すべてが「正常に動作したうえで素通し」になる。層の数を増やしても防げない唯一の経路だった
- **学び**: 多層防御の検査項目は「各層が生きているか」だけでなく、**全層が共有している前提(単一ソース)が生きているか**を含める

**割り切りの受け皿が実在するかを確認していなかった。**

- 計画は残る穴 3 件のうち 2 件を「リモートのブランチ保護設定」に集約していたが、このリポジトリは Free プランの private で**ブランチ保護もルールセットも使えない**(`403 Upgrade to GitHub Pro`)。受け皿は空だった
- **学び**: 「この穴は別の層が補う」と書いたら、**その層が実際に存在することを確認するまで割り切りは成立していない**。段階6 で `/kickoff` にプラン確認の導線を入れる

## フェーズ13: 8 巡目の監査で見つかった破綻要因の修正(追加)

- [x] `.husky/prepare-commit-msg` を新設し、`git revert` / `git cherry-pick` を保護ブランチ上でブロック(`pre-commit` は発火しないことを実測で確認)
- [x] 第 2 引数が `merge` のときは通す(保護ブランチで `git pull` ができなくなるのを避ける)
- [x] `check-branch-policy.sh` の検査対象に `git revert` / `git cherry-pick` を追加(`--abort` / `--quit` / `--skip` / `--continue` は除外)
- [x] 自壊検知の実体を `.claude/scripts/check-guard-integrity.sh` に集約(SessionStart / CI の判定ずれを防ぐ)
- [x] 「層まるごと消えたケース」の検知(`package.json` の husky 依存も判定材料にする)
- [x] 呼び出し検査を文字列一致から「コメントでない行からの起動」に変更
- [x] `lint-staged` の起動を 2 段構えに(`command -v` → `npx --no-install`)
- [x] `.husky/prepare-commit-msg` を template-manifest の `merge` に登録
- [x] git 操作の網羅テスト 10/10・自壊検知 9/9・新フックのフェイルオープン 4/4
- [x] 計画文書 §8 / CHANGELOG / README / lead ルール / development-guidelines / codex-harness.html を更新

## 8 巡目の監査(フェーズ13)で見つかったこと

**フック名を守備範囲だと思い込んでいた。**

- `pre-commit` という名前は「コミット前」と読めるが、実際に発火するのは `git commit` と `git commit --amend` **だけ**。`git revert` / `git cherry-pick` では発火しない(実測)
- つまりこの層が守っていたのは「コミットという**結果**」ではなく「`git commit` という**コマンド**」だった。層の名前(「保護ブランチへの直接コミットを止める」)と実際の守備範囲がずれていたが、名前が正しく見えるぶん気づけなかった
- Claude 側の `check-branch-policy.sh` も同じ正規表現の作りで `git commit` だけを見ており、**同じ思い込みが 2 層に写っていた**。ベンダー中立化のために「同じ判定を 2 経路へ写す」設計にしたことで、**欠落もそのまま複製されていた**
- **学び**: 「どの操作を守るか」はフック名や直感ではなく**実測**で確かめる。特に判定を複数経路へ写す設計では、写す前の判定が正しいかを先に検証する(誤りも等しく複製される)

**副次的な収穫**: `prepare-commit-msg` は `--no-verify` で迂回できない(`--no-verify` が無効化するのは `pre-commit` と `commit-msg` だけ)。残る穴として割り切っていた 3 件のうち 1 件が、ローカル層だけで塞がった。

**「X の存在を前提に X を検査する」を 3 回繰り返した。**

- フェーズ8: 共有スクリプトが消えると検査がスキップされた → 修正
- フェーズ12: `sh -e` で呼び出し側のフェイルオープンが無効化されていた → 修正
- フェーズ13: **`.husky/pre-commit` ごと消えると検査ブロック全体がスキップされた**
- 同じ構造を 3 回踏んだのは、そのつど「今回の修正対象」だけを見ていたため。**検査を書いたら「この検査自体が実行されない条件」を必ず列挙する**という手順にしないと再発する
- 併せて、呼び出し検査が文字列一致だったため**説明コメントにファイル名があるだけで緑になる**ことも判明した(今回コメントを厚くしたことで自分が踏みかけた)

## 9 巡目(2026-08-20 レビュー指摘の反映)

`docs/template-dev/vendor-neutral-guard-review.html` の指摘を確認し、以下を修正した。

- [x] **`$2=merge` の無条件素通しをやめ、`.git/MERGE_HEAD` の有無で判別する**(`.husky/prepare-commit-msg`)
- [x] `check-protected-branch.sh` のコメント修正(merge 中は detached HEAD にならない)
- [x] 「`git merge` / `git pull` の統合コミットは通す」→「取り込みだけを通す」に表現統一(README / lead ルール / development-guidelines / CHANGELOG / 計画文書 / codex-harness.html)
- [x] Claude 側の `--abort|--quit|--skip|--continue` 除外がコマンド文字列全体に当たる件を計画文書に明記(挙動は変えない。git hook 層が拾うため)

**レビューが拾えなかった穴を、指摘の裏取り中に実測で発見した(8 巡目と同じ構造の再発)。**

- `git revert -e` / `git cherry-pick -e` は第 2 引数に **`merge` を渡してくる**(git 2.53 で実測)。`pre-commit` は発火しないため、**`-e` を付けるだけでローカル 2 層をまとめてすり抜けられた**
- 8 巡目の学びは「フック名ではなく**どの操作で発火するか**を実測せよ」だったが、今回すり抜けたのは**同じフックに渡る引数の値**。「発火するか」は実測したが「**引数が期待どおりか**」は git の man を読んだ理解のまま通していた
- **学び**: 分岐の入力値も実測する。特に「この値のときだけ素通しする」という**例外側の分岐**は、素通し条件が広すぎてもテストが緑のままになるため気づけない。素通し分岐には「**それ以外で同じ値を渡してくる操作は無いか**」を必ず当たる
- 判別に使う値は、意味が一意に決まるもの(`.git/MERGE_HEAD` の存在 = 本当にマージ中)を選ぶ。第 2 引数は「メッセージの出所」であって「操作の種類」ではなかった

**検証**: 保護ブランチ上で revert / cherry-pick(既定・`-e` 双方)が BLOCKED、`git merge --no-ff` が ALLOWED、非保護ブランチでは全て ALLOWED であることを実リポジトリで確認済み。
