# 要件: 段階0 の残件(データガバナンス判断と §13 の決着)

- Issue: [#4](https://github.com/fuji18/claude-codex-template/issues/4)
- 根拠: `docs/template-dev/codex-delegation-plan.md` §10.2 / §11 / §13
- ブランチ: `feature/codex-stage-issues`(PR [#9](https://github.com/fuji18/claude-codex-template/pull/9) に相乗り)

## 背景

Issue #4 のスコープ 4 点のうち、着手時点で **2 点は既にこのブランチ上で完了している**:

| # | スコープ | 状態 |
| --- | --- | --- |
| 1 | データガバナンス判断(§10.2) | ❌ 未着手 |
| 2 | Codex CLI の導入(`post_create.sh`) | ✅ 完了(commit `c5331c8`) |
| 3 | §13 の実機検証(#4 / #5 / #6) | 🔶 #5 のみ完了(`c5331c8`)。#4 と #6 が残 |
| 4 | 段階2 の価値判定 | ✅ 完了・判定「続行」(commit `c7aee02`) |

**したがって今回やるのは 1 と 3 の残り**、およびそれに伴う文書・マニフェストの整合である。

## やること

1. **データガバナンス判断を下し、送信禁止パスを機械可読な単一ソースに外出しする**
   - 現状 `delegate-codex.sh` の入口検査1 はパターンが**スクリプトにハードコード**されており、§10.2 の「決めた一覧をスクリプトに渡す」が満たせていない
   - 現状の検査は `-maxdepth 3` かつ**ファイル名一致のみ**で、深い階層やパス指定の機密を取りこぼす
2. **§13 #4(`.codex/prompts/` の project スコープ)に結論を入れる** → 実機調査済み(design.md D4)
3. **§13 #6(`resetAt` の機械的取得)に結論を入れる** → 実機ログで判定済み(design.md D5)
4. **認証キャッシュ(`~/.codex/auth.json`)の永続化の可否を決める** → §13 #5 の代替欄が「#4 で判断する」と積み残していた
5. 上記の結論を計画文書(`.md` / `.html`)・`README.md`・`template-manifest.json` に反映する

## やらないこと

- `delegate-codex.sh impl` の実装(段階3 = #5)
- `.codex/skills/` の実機動作確認(段階5 = #7 に送る。理由は design.md D4)
- CI への Codex 導入(§10.6 で不採用が確定済み)
- **上限に到達させる実験**(枠を捨てることになる。#6 は「成功時のログに出ないこと」までで確定させる)

## 受け入れ条件(Issue #4 の条件に対応)

- [x] データガバナンス判断が下され、送信可能パス/送信禁止パスが文書化されている
- [x] 送信禁止パスの一覧が `delegate-codex.sh` から参照される単一ソースになっている
- [x] `codex` コマンドが devcontainer で実行でき、認証が通っている(済。`c5331c8`)
- [x] §13 の #4 / #5 / #6 に結論が入り、計画文書の表が更新されている
- [x] `explore` / `review` の実機完走と段階2 の価値判定が §11 に記録されている(済。`c7aee02`)
- [x] 入口検査1 の回帰確認が **Codex を呼ばずに**完了している(枠を消費しない)
- [x] `npm run lint` / `npm run format:check` が通る
