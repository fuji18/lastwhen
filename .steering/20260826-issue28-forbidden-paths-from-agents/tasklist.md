# タスクリスト: Issue #28

design.md の該当節を読んでから着手する。設計判断が必要になったら停止して報告する。

- [x] 1. `delegate-codex.sh`: 入口検査2・3 の直後にプロジェクト固有パスの抽出ブロックを追加(design.md §1-A)
- [x] 2. `delegate-codex.sh`: `forbidden_files()` をマージ対応 + グロブ表記対応に書き換え(§1-B)
- [x] 3. `delegate-codex.sh`: `forbidden_snapshot()` の `sort` を `sort -u` にし理由コメントを足す(§1-C)
- [x] 4. `delegate-codex.sh`: `FORBIDDEN_PATHS` 上のコメントと「空振り条件」を差し替え(§1-D / §1-E)
- [x] 5. `bash -n .claude/scripts/delegate-codex.sh` が通ることを確認
- [x] 6. `AGENTS.md` L145 の一文を差し替え(§2)
- [x] 7. `CLAUDE.md` の単一ソース説明を差し替え(§3)
- [x] 8. `.claude/commands/kickoff.md` フェーズ4-3 に 1 行追記(§4)
- [x] 9. 再現テスト シナリオ1〜6 を実行(§5)
- [x] 10. `.steering/20260826-issue28-forbidden-paths-from-agents/verification.md` に結果を記録(§5-3)
- [x] 11. テスト用の一時ファイル・run record を掃除し、`git status` が意図した差分のみになっていることを確認

---

## 振り返り(申し送り)

- **fork の往復 0 回。** design.md に置換後のコード全文(コメント文言込み)・検証シナリオ 6 件・
  後片付けまで書き切った形が #22・#27 に続いて 3 回連続で機能している
- **計画時に発見した設計上の穴が 2 つあり、どちらも実装者に判断させずに済んだ**:
  1. `sort -u`(§0.6)— 汎用項目とマーカー内は必ず重複するため、`sort | uniq -u` の違反抽出が
     取りこぼす。**既存コードを読まずに機能追加していたら踏んでいた**。既存の下流ロジックが
     入力の重複を前提にしているかは、リストをマージする変更で毎回確認する
  2. グロブ表記(§0.5)— `/kickoff` の記入例が `src/auth/**` 形式であることに気づかないと、
     下流プロジェクトで**抽出はされるが 1 件も実在せず、保護がゼロのまま静かに通る**。
     「書く側のフォーマット」を実際に読みに行くこと
- **`CODEX_DELEGATE_ACK_SECRETS=1` を design.md の検証手順に織り込めた**(#21・#22 で 2 回
  連続で取りこぼしていた項目)。スタブ検証を設計するときの定型として定着させる
- **残した既知の限界**: マーカー内のパスの typo は無音で無視される(warning を出すと
  説明用の語で毎回鳴るため不採用。verification.md §8)。下流で保護漏れが起きたときの
  最初の疑い先はここになる
