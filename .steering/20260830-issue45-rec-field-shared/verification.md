# 検証記録・振り返り: rec_field() の二重実装を共有ファイルに集約する(Issue #45)

## 1. 計画時に確定させたこと(司令塔)

計画フェーズで**設計判断をすべて潰してから**実装に渡した。design.md §0 に置いた「実装者が
再調査しなくてよい事実」が要点で、特に効いたのは次の 1 点:

> `ROOT` と `EX_FAIL` は入口検査より**前**(91 行 / 86 行)で確定している。

これが分かっていたので、`source` を旧 `rec_field` の位置(642 行 = 入口検査4 の後)ではなく
`cd "$ROOT"` の直後に置けた。結果として:

- **`--print-forbidden` を含む全起動経路**が共有ファイルの解決を通る
- 検証が **codex CLI にも機密承認にも依存しない**経路だけで組める(§3)

旧 `rec_field` の位置にそのまま置いていたら、codex CLI が無い環境では解決が一度も走らず、
壊れていても気づけないまま緑になっていた。

## 2. 自己コピー exec との整合(このチケットの本題)

`delegate-codex.sh` は起動直後に自身を一時ディレクトリへコピーして exec する(#15)。
共有ファイルを `$ROOT` から素朴に `source` すると、**「実行中に委託先が書き換えられるファイル」が
1 本増える** = この保護に穴が開く。

採った対策は **「コピー先へ `lib-record.sh` も一緒に運び、自身の隣から source する」**(design §1 案 A)。
検討して却下した案は 3 つ:

| 案 | 却下理由 |
| --- | --- |
| B: コピー時に本文を埋め込む | コピー処理が「運ぶ」から「合成する」に変わり失敗モードが増える。得るものは A と同じ |
| C: 実行前後でハッシュ照合 | **検知しかできない**。A は発生自体を防ぐ |
| D: `$ROOT` から素朴に source | #15 の保護に穴を開ける。委託先がハーネス層を触るのはテンプレート開発では常態 |

**フェイルの向きを 2 箇所で違えた**点が設計の勘所:

- **コピー失敗はフェイルオープン**(委託を止めない)。自己コピーは堅牢化の層であって安全検査ではない、
  という #15 の既存判断と揃えた。ただし `$ROOT` へフォールバックしたことは警告に出す
- **共有ファイルが 1 つも無い場合はフェイルクローズ**。`rec_field` は入口検査5-5(impl の再入防止)が
  使う。未定義のまま進むと空文字列が返って**検査が静かに素通しする**だけで、失敗として現れない。
  これはこのチケットが集約しようとしている「jq 不在時のフェイルオープン」と同じ穴の別形

## 3. 検証(design.md §7。必須 11 項目すべて実施)

実装者が実施し、`code-reviewer` が独立に再現した。

| 観点 | 結果 |
| --- | --- |
| `bash -n` 3 ファイル / `lib-record.sh` に実行ビット無し | pass |
| `rec_field` 単体(jq あり / なし)・存在しないキー・`accepted: false` | pass |
| **pid の末尾カンマが付かない**(過去の Critical の回帰点。jq なし経路) | pass |
| `codex-run.sh` の等価性 4 通り(`list --all` / `pending` × jq あり / なし)が計画時の基準出力と `diff` 完全一致 | pass |
| 自己コピー経路で共有ファイルが解決される(**フォールバック警告が出ない**ことで観測) | pass |
| `CODEX_DELEGATE_NO_SELF_COPY=1` でも動く | pass |
| `lib-record.sh` 退避時に `delegate-codex.sh` / `codex-run.sh` が両方 exit 2(フェイルクローズ) | pass |
| 退避状態で `check-guard-integrity.sh` が検知し、復元後は無出力に戻る | pass |
| フルスイート(lint / typecheck / test / format:check) | pass |

**jq を PATH から外す方法**: `/usr/bin/jq` は他の必須コマンドと同じディレクトリにあるため PATH からは
外せない。`command` を関数で覆って `export -f` する(design §7-1)。子スクリプトへ継承されることを
計画時に実測で確認してから設計に載せた。

### 未実施の 1 項目(design.md が任意と明示したもの)

- **入口検査5-5 の実経路**(§7-3): 実際の impl 委託で再入防止が働くことの確認。
  ワークツリーに `.claude/settings.local.json` があるため入口検査1 が `CODEX_DELEGATE_ACK_SECRETS=1` を
  要求する。**機密承認を伴う実行は検証のために回す性質のものではない**と判断し、計画段階から任意に
  倒してある。`rec_field` の振る舞いは §3 の単体検査で押さえてある

## 4. 検収(往復 0 回 / レビュー 1 巡)

`code-reviewer`: **0 Critical / 0 Major / 1 Minor**。`test-runner`: 全 pass。

**不採用にした Minor が 1 件**:

- 自己コピーブロックで導入した `_self_dir` を `unset` していない → **不採用**。
  同ブロックの既存変数 `_self` / `_copy_dir` も `unset` されておらず、既存スタイルと整合している。
  ハーネス中枢に「揃えるためだけの差分」を足す方がコストが高い

## 5. 申し送り

- **`--print-forbidden` の出力行数**: `FORBIDDEN_PATHS` 単体では 10 行だが、`AGENTS.md` §4 由来の
  `PROJECT_FORBIDDEN_PATHS`(#28)を含めた総出力は 22 行。design.md §7 が「10 行」と書いたのは前者の意。
  今後この経路の行数を検証条件に使うときは、どちらを指しているかを明示すること
- **jq 経路と sed 経路には既存の出力差がある**(design §6)。`codex-run.sh pending` のサマリー行で、
  `jq -r` は JSON 中の `\n` を実際の改行に戻すが sed 経路は生の 2 文字 `\n` を返す。
  **今回入れた差ではなく、現行 2 実装が最初から持っていた差**。したがって等価性検査は
  「jq 経路 vs sed 経路」ではなく**変更前の同じ経路との比較**で行った。直すなら run record の
  書式に手を入れる別チケットになる
- **`check-guard-integrity.sh` は変更していない**。`FORBIDDEN_PATHS` が `.claude/scripts/` を
  ディレクトリ単位で持つ(#40)ため、`lib-record.sh` は追加作業なしで出口検査・縮退検査の対象になる。
  個別に足すと #40 が畳んだ「ファイルが増えるたびに列挙が漏れる」問題を再導入する。
  同じ理由で `AGENTS.md` §4 と `CLAUDE.md` の禁止領域リストも変更不要
- **副次的な良い性質**: `lib-record.sh` が消えると `--print-forbidden` が空を返し、
  `check-guard-integrity.sh` が「委託禁止領域を返さない」と鳴る。**フェイルクローズが検知層まで
  貫通する**。意図した挙動なので、将来この経路を静かにフェイルオープンへ倒さないこと
- 途中でセッション上限により検収サブエージェント 2 本が中断したが、作業ツリーは無傷で、
  同じ内容で起動し直して完走した。**実装成果は `.steering/` と作業ツリーに残るため再開に支障はない**

---

## 6. 改訂: CI の実行ビット検査との衝突(往復 1 回)

PR #53 を出した後、CI `harness-integrity` が落ちた:

```
::error::hook スクリプトに実行権限がありません: .claude/scripts/lib-record.sh
```

`.github/workflows/ci.yml` と `.claude/hooks/session-start.sh` の 2 箇所が
**`.claude/scripts/*.sh` と `.claude/hooks/*.sh` は全部が実行可能であること**を要求しており、
`lib-record.sh` を source 専用(実行ビットなし)にした設計と正面から衝突した。

### 計画時に何を見落としたか

「新しい共有ファイルを `.claude/scripts/` に置く」と決めた時点で、**そのディレクトリを
走査している既存の検査を洗い出すべきだった**。禁止領域(`FORBIDDEN_PATHS`)がディレクトリ単位で
自動的に効くことは確認していた(design §5)のに、**同じディレクトリを見ている実行ビット検査は
見ていない**。ディレクトリ単位のルールは味方にも敵にもなる。

### 修正方針(実行ビットを付けて黙らせない)

`git update-index --chmod=+x` で CI は通るが採らなかった。shebang も持たない source 専用ファイルに
実行ビットを付けるのは性質について嘘をつくことになり、検査の目的(hook の実体が実行不能になって
PreToolUse がフェイルオープンする、の検知)とも合わない。

検査側に **「実行される実体 / source 専用ライブラリ」の区別**を入れ、命名規約を `lib-*.sh` とした。
**ただし単なる免除にはしていない** — 免除だけだと hook の実体を `lib-` にリネームするだけで
検査を逃れられる。ライブラリ側には別の縛りを課した:

| 種別 | 実行ビット | shebang | git index mode |
| --- | --- | --- | --- |
| `lib-*.sh` | **無いこと** | **無いこと** | `100644` |
| それ以外 | 有ること(従来どおり) | 問わない | `100755` |

**検査に例外を足すときは、例外側にも検証可能な要件を課さないと抜け道になる。**

### 検証

- `ci.yml` のループをローカルで `bash -e` 再現 → 無出力・exit 0(司令塔が確認)
- 抜け道テスト: `lib-record.sh` に実行ビットを付ける / shebang を足す → **どちらも落ちる**
- 既存検査の生存: `harness-mode.sh` の実行ビットを外す → 従来の文面で落ちる
- git index mode: `lib-record.sh` が `100644`、他の `*.sh` は全て `100755`
- 既存のエラー文面・警告文面は 1 文字も変えていない(分岐が増えただけ)
