# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

---

## フェーズ1: denylist の更新

- [x] `.claude/codex-denylist.txt` を `design.md`「変更後の `.claude/codex-denylist.txt` 全文」の内容で置き換える
  - [x] 既存 10 パターンが 1 つも消えていないことを目視確認(`.env` / `.env.*` / `*.pem` / `*.key` / `id_rsa*` / `id_ed25519*` / `credentials*` / `*.p12` / `*.pfx` / `.claude/settings.local.json`)
  - [x] 追加 11 パターンがすべて入っていることを目視確認(`.npmrc` / `.pypirc` / `.netrc` / `.git-credentials` / `*-service-account*.json` / `*.tfstate` / `*.tfstate.backup` / `*.sqlite` / `*.sqlite3` / `*.db` / `dump*.sql`)
  - [x] 行末コメントを書いていないこと(`#` 以降は切り捨てられるため、注記は独立行)

## フェーズ2: 動作確認(design.md「データフロー(検証手順)」に従う)

**⚠️ 検証は必ず存在しない target(`.steering/__verify_nonexistent__`)に対して行う。実在の
ステアリングディレクトリを渡すと実際の委託が起動して Codex の枠を消費する。**

- [x] 検証1: 追加パターン 11 種を空ファイルで置き、`exit=2` かつ 11 件すべてが列挙されることを確認
- [x] 検証1 の一時ファイルを削除
- [x] 検証2: `.env.example` / `.npmrc.sample` / `app.db.template` を置き、「機密の可能性があるファイル」が**出ない**ことを確認(止まるのは検査5 のメッセージ)
- [x] 検証2 の一時ファイルを削除
- [x] 検証3: `.env` / `server.pem` / `deploy.key` / `credentials.json` を置き、`exit=2` かつ 4 件すべてが列挙されることを確認(既存パターンの回帰確認)
- [x] 検証3 の一時ファイルを削除
- [x] 検証4: 一時ファイルが無い状態で「機密の可能性があるファイル」が出ないことを確認(通常のワークツリーが常時ブロックされない)
  - 注: この devcontainer には既存(未変更)パターン `.claude/settings.local.json` に一致する実ファイルが常時存在し、一時ファイル無しでも `exit=2` になる。これは本タスクで追加したパターンとは無関係で、変更前から同じ挙動だったことを確認済み(振り返りに記録)
- [x] 検証5: `git status --porcelain` で、`.claude/codex-denylist.txt` と `.steering/20260826-issue21-denylist-patterns/` 以外のエントリが無いことを確認

## フェーズ3: 品質チェックと修正

- [x] `/check`(test-runner に委譲)の全チェックがパスすることを確認
  - 検証コマンドは `package.json` の scripts が正(`lint` / `format:check` / `typecheck` / `test`)
  - 変更ファイルは `.claude/codex-denylist.txt`(テキストファイル)のみのため、`lint` / `typecheck` / `test` は対象外。`npx prettier --check .claude/codex-denylist.txt` で整形確認済み(合格)

## フェーズ4: ドキュメント更新

- [x] README.md の更新要否を判断する(denylist の内容は README に列挙されていないため、原則不要。参照している記述があれば更新する)
  - README.md L227 は denylist の仕組みの説明のみでパターン列挙は無いため、更新不要と判断
- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-08-26

### 計画と実績の差分

**計画と異なった点**:
- 検証4(通常のワークツリーが常時ブロックされないこと)で、この devcontainer には
  既存(未変更)パターン `.claude/settings.local.json` に一致する実ファイルが常時存在するため、
  一時ファイルを何も置いていない状態でも `exit=2` になった。この動作は本タスクで追加した
  パターンとは無関係で、変更前の 10 パターンの denylist でも同じ挙動になることを確認済み
  (`.claude/settings.local.json` パターンは元から存在し、今回の変更で触れていない)。
  設計との矛盾ではなく、この開発環境固有の条件による差異と判断し、そのまま進めた

**新たに必要になったタスク**:
- なし

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- なし

### 学んだこと

**技術的な学び**:
- `.claude/settings.local.json` は `.gitignore` 済みのローカル設定ファイルで、
  devcontainer 環境には実体があるため denylist の「通常時ブロックなし」検証は
  実行環境に依存する

**プロセス上の改善点**:
- 検証手順を tasklist に沿って 1 つずつ実行し、都度一時ファイルを削除したことで
  `git status --porcelain` が最終的にクリーンな状態を保てた

### 次回への改善提案
- denylist 関連の検証手順を設計するときは、devcontainer にローカル設定ファイル
  (`.claude/settings.local.json` 等)が実在する前提で期待値を書くとより正確になる
