# タスクリスト: §12.7 入口検査のランブック追加

- [x] 1. `.claude/scripts/delegate-codex.sh` の入口検査1・2・3・5-1〜5-5 のブロックを読み、各検査の終了コードと「空振りする条件」のコメントを拾う
- [x] 2. `docs/template-dev/codex-delegation-plan.md` の §12.6 の直後に §12.7 を追加する(design.md の表構成に従う)
- [x] 3. 追加した節の書式(見出しレベル・である調・表の列数)が既存節と揃っていることを確認する

## 申し送り事項

**実施日**: 2026-08-23 / **実装者**: 実機の Codex(CLI v0.149.0、`chatgpt` モード)

これは Issue #5 の受け入れ条件 #1・#2 を満たすための**実機委託**であり、成果物(計画文書 §12.7)も本番に採用した。

| 見た点 | 結果 |
| --- | --- |
| 完走したか | ✅ tasklist 3/3 を**逐次更新**し `exit 0` |
| 所要 | 2 分 14 秒(13:34:18 → 13:36:32) |
| サマリーだけ返るか | ✅ 生ログ 66,347 B → 司令塔へ約 60 B(**0.09%**) |
| 成果実在確認を通ったか | ✅ 2 ファイル変更・tasklist の `[x]` が 3 件増加 |
| `codexSessionId` を取れたか | ✅ `01a02ed4-…`(`thread_id`)。§13 #6 で取れないと確定していたのは `resets_at` の方 |
| 内容の妥当性 | ✅ 6 検査すべての終了コードと空振り条件がスクリプト実装と一致。**修正なしで採用** |

**入口検査1(機密)が `.claude/settings.local.json` を検出したため、`CODEX_DELEGATE_ACK_SECRETS=1` が必要だった。** このファイルは gitignore 済みだが denylist に一致する。委託のたびに承認が要る状態なので、段階4 以降で denylist の粒度(ローカル設定を除外してよいか)を判断する余地がある。

**未実施**: `--background`(段階4)。同期実行のみで検証している。
